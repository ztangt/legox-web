import React from 'react';
import IconConfig from './componments/iconConfig';

export default () => {
    return (
            <div id='iconConfig_container' style={{ height: '100%' }}>
                <IconConfig />
            </div>
    );
}