import React from 'react';
import EntranceConfig from './componments/entranceConfig';

export default () => {
    return (
            <div id='entranceConfig_container' style={{ height: '100%' }}>
                <EntranceConfig />
            </div>
    );
}